apiVersion: v1
kind: Service
metadata:
  name: {{ include "opc-platform.fullname" . }}
  labels:
    {{- include "opc-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opc-platform.selectorLabels" . | nindent 4 }}
